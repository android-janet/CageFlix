<h1> New Review </h1>

<%= form_for @review do |f| %>

<%= f.collection_select :movie, :movie_id, :id, :name %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :body %>
<%= f.text_area :body %>

<%= f.submit 'Submit Review' %>

<% end %>
